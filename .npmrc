# Use bun registry (optional; bun uses npm's registry by default)
registry=https://registry.npmjs.org/

# Set the scope to publish publicly
@sanDeep-jaiswar:registry=https://registry.npmjs.org/
access=public

# Authenticate if needed (replace with your actual token if using CI/CD)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
